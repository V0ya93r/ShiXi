##### 1在已有项目基础上移植RTOS源码
- User文件夹中存放FreeRTOSConfig.h，为配置文件
- Middlewares里新建FreeRTOS存放源码和内存管理文件。Middlewares/FreeRTOS/src是源码，Middlewares/FreeRTOS/port是内存管理文件。
- 之后修改FreeRTOSConfig.h等
- 配置可以直接用CubeMX配置好带有FreeRTOS架构的代码
##### 任务创建
- include "FreeRTOS.h"、"task.h"、"queue.h"等头文件
- 任务句柄TaskHandle_t StartTask_Handler/Task1_Handler；队列句柄QueueHandle_t KeyQueue_Handler（如果后续不对其进行操作例如挂起删除，则不用定义）
- 声明任务void start_task(void *vpParameters)
- 创建任务xTaskCreate(指向任务的指针，任务名，堆栈大小，传递参数NULL，优先级，任务句柄或NULL)
- 启动任务调度器vTaskStartScheduler()
- 定义具体task